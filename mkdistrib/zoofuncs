
addtext () {
    archivename=$1
    shift 
    ${BINDIR}/saytext | zoo ahc ${archivename}.zoo $* >& /dev/null &
    wait
}

addbin () {
    archivename=$1
    shift 
    ${BINDIR}/saybin | zoo ahc ${archivename}.zoo $* >& /dev/null & 
    wait
}
