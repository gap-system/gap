# We test this here to avoid problems with filenames

# Use AssertionLevel as it is small so hopefully
# won't change or move very often
Display(AssertionLevel);
Print(AssertionLevel);
f := {x} -> x;
Display(f);
Print(f);
