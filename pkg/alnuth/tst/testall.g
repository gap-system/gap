LoadPackage("alnuth");
LoadPackage("radiroot");
ReadTest(Filename(DirectoriesPackageLibrary("alnuth", "tst"), "version.tst"));
ReadTest(Filename(DirectoriesPackageLibrary("alnuth", "tst"), "ALNUTH.tst"));
ReadTest(Filename(DirectoriesPackageLibrary("alnuth", "tst"), "examples.tst"));
ReadTest(Filename(DirectoriesPackageLibrary("alnuth", "tst"), "manual.tst"));
ReadTest(Filename(DirectoriesPackageLibrary("alnuth", "tst"), "polynome.tst"));

